{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local frontend with host-owned native commands; updater UI remains disabled.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
