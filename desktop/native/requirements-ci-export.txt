# Install torch==2.9.1 separately from https://download.pytorch.org/whl/cpu.
# CI research/evaluation only; no exported weights are release artifacts.
-r requirements.txt
onnx==1.20.1
# Synthesis/legacy conversion do not import the separate training augmentation stack.
requests==2.32.4
click==8.2.1
